apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pedidos-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-pedidos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-pedidos
  template:
    metadata:
      labels:
        app: db-pedidos
    spec:
      containers:
        - name: db-pedidos
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              value: pedidos
          volumeMounts:
            - name: datos
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: datos
          persistentVolumeClaim:
            claimName: db-pedidos-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-pedidos
spec:
  selector:
    app: db-pedidos
  ports:
    - port: 5432
      targetPort: 5432