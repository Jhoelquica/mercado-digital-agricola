apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-notificaciones-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-notificaciones
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-notificaciones
  template:
    metadata:
      labels:
        app: db-notificaciones
    spec:
      containers:
        - name: db-notificaciones
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              value: notificaciones
          volumeMounts:
            - name: datos
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: datos
          persistentVolumeClaim:
            claimName: db-notificaciones-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-notificaciones
spec:
  selector:
    app: db-notificaciones
  ports:
    - port: 5432
      targetPort: 5432