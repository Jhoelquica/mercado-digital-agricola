apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-transporte-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-transporte
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-transporte
  template:
    metadata:
      labels:
        app: db-transporte
    spec:
      containers:
        - name: db-transporte
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: credenciales-app
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              value: transporte
          volumeMounts:
            - name: datos
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: datos
          persistentVolumeClaim:
            claimName: db-transporte-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-transporte
spec:
  selector:
    app: db-transporte
  ports:
    - port: 5432
      targetPort: 5432
